class Image {
  constructor(id, referencia, titulo, dataCriacao) {
    this.id = id;
    this.referencia = referencia;
    this.titulo = titulo;
    this.dataCriacao = dataCriacao;
  }
}

module.exports = Image;
